/**
 * ================================================================
 * 파일명       : constants/index.ts
 * 목적         : 프로젝트 전역 상수 정의
 * 설명         : 
 *   - 타이밍 관련 상수 (애니메이션, 딜레이)
 *   - 크기/거리 관련 상수
 *   - UI 설정 상수
 * ================================================================
 */

// =====================================
// 타이밍 상수 (밀리초)
// =====================================

/** 애니메이션 지속 시간 (줌/팬 전환) */
export const ANIMATION_DURATION = 400;

/** 토스트 메시지 표시 시간 */
export const TOAST_DURATION = 3000;

/** 태스크 추가 후 선택 대기 시간 (시뮬레이션 안정화) */
export const TASK_SELECT_DELAY = 600;

/** 자동 팬 주기 (~60fps) */
export const AUTO_PAN_INTERVAL = 16;

// =====================================
// 크기/거리 상수
// =====================================

/** 화면 경계 감지 영역 (자동 팬용) */
export const EDGE_THRESHOLD = 80;

/** 자동 팬 속도 */
export const PAN_SPEED = 8;

/** 드래그 인식 임계값 (픽셀) */
export const DRAG_THRESHOLD = 3;

/** 중앙 이동 스킵 거리 (이미 중앙이면 이동 안 함) */
export const CENTER_SKIP_DISTANCE = 10;

// =====================================
// 줌 제한
// =====================================

/** 최소 줌 레벨 */
export const MIN_ZOOM = 0.3;

/** 최대 줌 레벨 */
export const MAX_ZOOM = 3;

/** 줌 단계 (휠/버튼) */
export const ZOOM_STEP = 0.1;

/** 줌 버튼 단계 */
export const ZOOM_BUTTON_STEP = 0.2;

// =====================================
// 미니맵 설정
// =====================================

/** 미니맵 크기 */
export const MINIMAP_SIZE = { width: 150, height: 100 };

/** 미니맵 패딩 */
export const MINIMAP_PADDING = 20;

// =====================================
// 그리드 설정
// =====================================

/** 배경 그리드 크기 */
export const GRID_SIZE = 40;

